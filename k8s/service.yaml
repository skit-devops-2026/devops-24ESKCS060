apiVersion: v1
kind: Service
metadata:
  name: weather-dashboard-service
  namespace: default
  labels:
    app: weather-dashboard
spec:
  type: NodePort
  selector:
    app: weather-dashboard
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
      name: http
